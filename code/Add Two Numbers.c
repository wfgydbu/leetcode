/**
 * Definition for singly-linked list.
 * struct ListNode {
 *     int val;
 *     struct ListNode *next;
 * };
 */
 
#include <stdio.h>
#include <string.h>
#include <stdlib.h>

typedef struct {
	int val;
	struct ListNode *next;
}ListNode;

struct ListNode* addTwoNumbers(struct ListNode* l1, struct ListNode* l2) {
    ListNode* res;// = malloc(sizeof(l1));
    return res;
}

int main(){
	
	return 0;
}